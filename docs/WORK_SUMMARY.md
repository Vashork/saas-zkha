# Work summary for `audit-dashboard-fixes`

Дата: 2026-06-24
Ветка: `audit-dashboard-fixes`
PR: `#1`

Единое самари по работе в ветке. Подробный план для продолжения завтра вынесен в `docs/TOMORROW_HANDOFF.md`.

## 1. Уже исправлено

### Dashboard

- Исправлен список месяцев и выбранный период.
- Исправлен расчёт `Начислено / Оплачено / Всего к оплате / Из них просрочено`.
- Частичная оплата больше не скрывает долг.
- Fixed 3000 ₽ / paid 2000 ₽ показывает остаток 1000 ₽.
- Карточки переименованы: `Всего к оплате` включает просрочку, `Из них просрочено` является подмножеством общего долга.
- Variable-платежи без суммы теперь не считаются оплаченными: они отображаются как `ожидает начисления` или `просрочено, нет суммы`.
- Верхние карточки dashboard считают строки без суммы в количестве, но не добавляют их к рублёвой сумме, пока начисление неизвестно.

Файлы:

- `app/web/routes/dashboard.py`
- `app/web/templates/dashboard.html`

### Scheduler

- Генерация платежей стала идемпотентной по каждому подрядчику.
- Если за месяц уже есть один `Payment`, остальные подрядчики больше не пропускаются.
- При старте приложения добавлен одноразовый запуск генерации текущего месяца.
- Для variable-подрядчиков scheduler создаёт открытый платёж без суммы, который дальше нужно заполнить вручную или через Telegram.

Файл:

- `app/scheduler.py`

### Payments

- Добавлен selector периода.
- Можно открыть прошлый месяц и вернуться в текущий.
- Фильтры работают по effective-status.
- При создании можно выбрать статус: `pending`, `paid`, `overdue`.
- При смене `paid` на `pending/overdue` очищаются `paid_amount` и `paid_date`.
- Чек предлагается только для `paid`.
- Скачивание старого чека показывается только если effective-status = `paid`.
- Variable-платежи без суммы видны в списке, фильтрах `Ожидает/Просрочено` и помечаются как `требуется сумма`.
- Добавление, редактирование и удаление возвращают пользователя в тот же период.
- Write-действия закрыты DB-backed admin check.

Файлы:

- `app/web/routes/payments.py`
- `app/web/templates/payments.html`

### History

- История стала read-only по чекам: только скачивание.
- Скачивание чека показывается только для effective-status `paid`.
- История использует effective-status как dashboard/payments.
- Добавлен столбец `Остаток`.
- CSV export использует planned/paid/remaining/effective-status.
- Variable-платежи без суммы отображаются как `требуется сумма / внести начисление`.

Файлы:

- `app/web/routes/history.py`
- `app/web/templates/history.html`

### Auth

- Добавлен подписанный session cookie.
- Пользователь загружается из БД.
- Проверяется `User.is_active`.
- Guards больше не доверяют `user_role` / `page_permissions` из cookies.
- Login после обычного выхода или повторного входа всегда открывает dashboard `/`.
- Session cookie теперь подписывается с per-process boot-id, поэтому после рестарта web-приложения старая сессия становится недействительной и пользователь должен попасть на login.

Файлы:

- `app/web/routes/auth.py`
- `app/web/templates/login.html`
- `app/web/templates/settings.html`

### Settings

- Блок `Создать пользователя` свернут в раскрываемый `<details>`, потому что используется редко и занимал много места.
- В `docs/TOMORROW_HANDOFF.md` зафиксирован план по новым настройкам, администрированию вложений и backup.

Файлы:

- `app/web/templates/settings.html`
- `docs/TOMORROW_HANDOFF.md`

### Contractors

- Write routes закрыты DB-backed admin check.
- Доступ больше не решается через cookie role.

Файл:

- `app/web/routes/contractors.py`

### Analytics

- Годовой selector больше не строится от выбранного года.
- После выбора 2024/2022 в списке не пропадает 2026.
- Исправлена single-month логика previous/current.
- `Расходы по месяцам` теперь показывает только один выбранный календарный год, без второй серии предыдущего года.
- Сравнение вынесено в отдельный выпадающий список `Сравнить с`.
- По умолчанию используется текущий год против предыдущего; 1 января 2027 это будет 2027 против 2026.
- Можно выбрать любые годы из списка доступных годов.
- Если выбран конкретный месяц, заголовок переключается на `Расходы: <месяц> <год>`, а сравнение с выбранным compare-year явно остаётся в верхней карточке.

Файлы:

- `app/web/routes/analytics.py`
- `app/web/templates/analytics.html`

### Telegram bot

- Исправлен Docker entrypoint: `python -m app.bot.main`.
- Telegram document валидируется через `is_allowed_file`.
- Теги читаются и из текста, и из подписи к фото/документу.
- Поддерживается текущий месяц:

```text
#оплачено #slug #сумма:3200
```

- Поддерживается явный период:

```text
#оплачено #slug #сумма:1000 #период:2026-06
```

- Добавлен интерактивный сценарий без тегов: пользователь присылает файл/фото, бот спрашивает подрядчика, сумму, период, подтверждение и сохраняет оплату.
- Добавлена команда `/cancel`.

Файлы:

- `docker/Dockerfile.bot`
- `app/bot/parsers.py`
- `app/bot/handlers.py`
- `app/bot/interactive.py`
- `app/bot/main.py`

## 2. QA статус

Пользователь проверил:

- `Платежи`: у `Ожидает` / `Просрочено` не показываются иконки скачивания/загрузки чека — OK.
- `История`: чек виден только у `Оплачено`, история read-only — OK.
- `Login`: обычный перелогин открывает dashboard — OK.
- `Restart session`: после рестарта контейнера сессия слетает — OK.
- `Analytics`: основной год и год сравнения работают — OK.
- `Variable-подрядчик`: dashboard/payments/history/редактирование суммы — OK.

Что ещё нужно проверить:

- Верхние карточки dashboard после правки количества: `2 открытых платеж(ей), включая 1 без суммы`.
- Settings: блок `Создать пользователя` свернут и раскрывается.
- Telegram bot QA пока пропущен и будет проверяться позже общим пакетом.

## 3. Важное ограничение модели платежей

Сейчас `Payment` — это одна строка на `contractor/year/month`.

Она умеет показать начислено, оплачено, остаток, просрочку, один чек в `receipt_file`, а также состояние variable-платежа без суммы.

Для полноценной истории частичных оплат и нескольких чеков нужен следующий этап:

```text
PaymentPeriod
PaymentTransaction
```

## 4. Что можно делать сейчас

### Web UI

- Смотреть долги и просрочки.
- Видеть variable-подрядчиков без суммы как `ожидает начисления` / `просрочено, нет суммы`.
- Выбирать месяц на странице `Платежи`.
- Создавать и редактировать платежи за выбранный месяц.
- Менять статус.
- Прикладывать и скачивать чек только для effective-status `paid`.
- Смотреть историю как read-only журнал с фильтрами, остатком, чеками и CSV.
- После логина открывать dashboard.
- После рестарта web-приложения требовать новый login.
- В аналитике выбирать основной год и отдельный год сравнения.

### Telegram

- Оплата текущего месяца тегами.
- Оплата старого периода тегом `#период:YYYY-MM`.
- Интерактивный сценарий без тегов.

## 5. QA текущего пакета

1. `git pull origin audit-dashboard-fixes`.
2. `docker compose up -d --build`.
3. Проверить dashboard card counters: строки без суммы должны учитываться в количестве, но не в рублёвой сумме.
4. Проверить settings: `Создать пользователя` свернут и раскрывается.
5. Проверить Telegram тегами без файла.
6. Проверить Telegram тегами в подписи к файлу/фото.
7. Проверить Telegram со старым периодом.
8. Проверить Telegram без тегов: файл/фото → подрядчик → сумма → период → подтверждение.
9. Проверить `/cancel`.

## 6. Технический долг / следующий порядок

Подробно расписано в `docs/TOMORROW_HANDOFF.md`.

Коротко:

1. Администрирование прикреплённых файлов.
2. Backup MVP: `scripts/backup.sh` + `docs/RESTORE.md`.
3. Web UI для backup.
4. CSRF для POST-форм.
5. `COOKIE_SECURE` для production.
6. Alembic вместо ручных миграций.
7. PaymentTransaction ledger.
8. Автотесты.
