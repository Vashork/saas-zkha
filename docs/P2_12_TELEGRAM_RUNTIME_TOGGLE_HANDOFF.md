# P2-12 Telegram runtime toggle handoff — applied

This file used to contain a temporary handoff because the GitHub connector blocked the initial web-route patch.

Current branch state:

- DB-backed `telegram_bot_enabled` runtime toggle is implemented.
- Web UI saves and shows the runtime bot toggle.
- DB-backed per-command toggles are implemented for managed commands.
- Web UI saves managed command toggles.
- Regression coverage was added in Telegram UI/runtime management tests.

This file is intentionally retained as a short historical note only. It no longer contains instructions to apply.
