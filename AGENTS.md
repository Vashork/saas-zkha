# AGENTS.md — ZhKH Bot

Единый handoff-файл проекта для ветки `audit-dashboard-fixes`.

Старые документы `README.md`, `PLAN.md`, `PLAN_BATCH2.md`, `TODO.md` были объединены сюда и удалены из ветки.

---

## 1. Проект

**ZhKH Bot** — локальная система учёта коммунальных платежей ЖКХ.

Основные функции:

- справочник подрядчиков;
- генерация ежемесячных платежей;
- фиксация оплат через веб-интерфейс;
- загрузка и хранение чеков;
- дашборд;
- история платежей;
- аналитика;
- настройки пользователей и прав;
- локальные бекапы;
- отдельный Telegram-бот для фиксации оплат.

Рабочая ветка: `audit-dashboard-fixes`.

---

## 2. Архитектура

Проект запускается через Docker Compose.

Сервисы:

- `zhkh-web` — FastAPI + Jinja2;
- `zhkh-nginx` — reverse proxy, порт `80:80`, проксирует в `web:8000`;
- `zhkh-bot` — Telegram-бот на aiogram.

Runtime data:

- `./data` монтируется в `/app/data` и содержит SQLite DB + uploads;
- `./backups` монтируется в `/app/backups` и содержит локальные архивы;
- `./logs` монтируется в `/var/log/zhkh-bot`.

---

## 3. Финальное состояние ветки `audit-dashboard-fixes`

### Выполнено

- Signed session cookie `session`.
- `_require_page()` читает активного пользователя и права из БД.
- Display-only legacy cookies не используются для авторизации.
- Login проверяет активного пользователя.
- Обычных пользователей можно удалять.
- Админов нельзя удалять, их можно только деактивировать.
- Самого себя деактивировать нельзя.
- При запрете страницы пользователь видит уведомление “нет доступа”.
- `init_db.py` больше не создаёт дефолтного обычного пользователя `user`.
- `init_db.py` создаёт только bootstrap-админа.
- Миграции в `init_db.py` больше не используют raw `cursor()`.
- Contractor write-actions ограничены admin-only.
- Суммы подрядчиков парсятся через `Decimal`, не через `float`.
- Ручное создание variable payment без суммы разрешено.
- Подрядчики с платежами не удаляются физически, а переносятся в архив.
- Основной список `/contractors` показывает только активных подрядчиков.
- Архив подрядчиков доступен через `/contractors?archived=1`.
- Архивного подрядчика можно вернуть в активные.
- Подрядчик без платежей удаляется физически.
- Dashboard безопасно парсит `year` и `month`.
- Dashboard показывает:
  - начислено за выбранный месяц;
  - оплачено за выбранный месяц;
  - к оплате за выбранный месяц;
  - всего к оплате по всем месяцам;
  - просрочено по всем месяцам.
- Dashboard показывает открытые долги и просрочки глобально по всем месяцам.
- History CSV export использует те же фильтры, что страница истории: год, месяц, подрядчик, статус.
- Scheduler запускает генерацию платежей при старте как repair-step.
- Scheduler по расписанию обновляет просроченные платежи.
- `/backups` добавлен как admin-only web UI.
- Backup архивирует каталог `data/`, то есть SQLite-базу, платежи, подрядчиков, пользователей, настройки и загруженные чеки.
- Backup-файлы можно скачать.
- Backup-файлы можно восстановить из списка.
- Внешний backup-файл можно загрузить, провалидировать и восстановить.
- Перед восстановлением создаётся страховочный backup текущего `data/`.
- Restore корректно работает с Docker volume mount `/app/data`: очищает содержимое папки, но не удаляет сам mount-point.
- Настройки backup в GUI: лимит хранения, частота, время запуска.
- Backend route для удаления backup-файла добавлен: `POST /backups/discard/{filename}`.
- При удалении платежа связанный чек удаляется из `data/uploads`.
- При замене чека старый файл удаляется из `data/uploads`.
- Добавлен admin-only механизм очистки сиротских чеков на `/payments`: кнопка `Очистить сиротские чеки`.
- `base.html` добавляет пункт `Бекапы` в navbar для admin, если конкретный шаблон его не вывел. Это workaround для analytics.

### Не получилось через connector / нужно доделать локально или другим агентом

- Кнопка удаления backup-файла в `backups.html` не записалась через connector: route готов, но UI-кнопка заблокировалась при записи шаблона.
- Прямая правка `analytics.html` для navbar блокировалась. Временный workaround сделан через `base.html`.
- Подключение частоты backup к APScheduler в `app/scheduler.py` блокировалось.
- Production hardening в `app/config.py` и `.env.example` блокировался: нужен warning/fail-fast для небезопасных дефолтов.
- CSRF-токены для POST-форм не добавлены.
- Telegram-бот не дорабатывался намеренно.

---

## 4. Roadmap / статус

### Этап 1 — критичные исправления запуска и данных

- [x] Переписать миграции в `init_db.py` без raw `cursor()`.
- [x] Убрать автосоздание дефолтного обычного пользователя `user`.
- [x] Проверено QA: после рестарта `user` больше не появляется.

### Этап 2 — `/backups` UI и restore

- [x] Добавить кнопку скачивания backup-файла.
- [x] Исправить текст страницы: архивы сохраняются в `./backups` на хосте через Docker volume.
- [x] Явно указать в GUI, что в backup входят платежи, подрядчики и прочие данные из SQLite DB.
- [x] Добавить настройку лимита хранения последних архивов.
- [x] Добавить настройку частоты backup и времени запуска в GUI.
- [x] Добавить кнопку восстановления у существующего архива.
- [x] Добавить загрузку внешнего backup-файла с валидацией.
- [x] Перед восстановлением создавать страховочный backup текущих данных.
- [x] Исправить restore для Docker volume.
- [x] Backend route для удаления backup-файла: `POST /backups/discard/{filename}`.
- [!] UI-кнопка удаления backup-файла в `backups.html` не добавлена: connector блокирует запись шаблона. Нужно добавить локально.
- [!] Подключить частоту backup к APScheduler: connector блокирует запись `scheduler.py`.

Локальная подсказка для кнопки backup-файла:

```html
<form method="post" action="/backups/discard/{{ f.name }}" onsubmit="return confirm('{% if backup_files|length == 1 %}Это последний локальный бекап. Удалить его всё равно?{% else %}Удалить файл бекапа {{ f.name }}?{% endif %}')">
    <button type="submit" class="btn-secondary-custom">Удалить</button>
</form>
```

Вставить рядом со `Скачать` и `Восстановить` в таблице `app/web/templates/backups.html`.

Локальная подсказка для scheduler:

- читать `backup_frequency`, `backup_time`, `backup_retention_count` из `settings`;
- если `manual` — не планировать job;
- если `daily`/`weekly`/`monthly` — создать cron job;
- job вызывает `create_local_backup()` из `app.backup_service`, затем `cleanup_old_backups()`;
- результат писать в `BackupHistory`.

### Этап 3 — навигация

- [x] Dashboard navbar содержит `Бекапы`.
- [x] History navbar содержит `Бекапы`.
- [x] Payments navbar содержит `Бекапы`.
- [x] Contractors navbar содержит `Бекапы`.
- [x] Settings navbar содержит `Бекапы`.
- [x] Analytics получает `Бекапы` через workaround в `base.html`.
- [!] Прямую правку `analytics.html` сделать локально, если нужен чистый шаблон без workaround.
- [ ] В будущем вынести navbar в общий include.

### Этап 4 — пользователи и безопасность операций

- [x] Вернуть удаление обычных пользователей.
- [x] Запретить удаление пользователей с ролью admin на backend-уровне.
- [x] Оставить деактивацию admin как основной сценарий.
- [x] Показывать уведомление “нет доступа” при переходе на запрещённую страницу.
- [ ] Позже добавить CSRF-токены для POST-форм.

### Этап 5 — деньги, подрядчики, платежи, чеки

- [x] Заменить `float` на `Decimal` при работе с `fixed_amount` подрядчиков.
- [x] Разрешить ручное создание variable payment без суммы.
- [x] Привести CSV export истории к тем же фильтрам, что страница истории.
- [x] Архив подрядчиков вместо физического удаления подрядчиков с платежами.
- [x] Отдельная вкладка `Архив` для подрядчиков.
- [x] Удаление платежа удаляет связанный чек.
- [x] Замена чека удаляет старый файл.
- [x] Массовая очистка сиротских чеков из `data/uploads`.

### Этап 6 — production hardening

- [!] Проверить поведение при небезопасных дефолтах конфигурации: не сделано через connector.
- [!] Добавить warning или fail-fast для production-сценария: не сделано через connector.
- [x] Описать restore-процедуру для backup.

Restore из backup через CLI:

```bash
docker compose down
mkdir -p data
cp -a data data.before-restore.$(date +%Y%m%d-%H%M%S)
tar -xzf ./backups/zhkh-data-backup-YYYYMMDD-HHMMSS.tar.gz -C .
docker compose up -d --build
docker logs zhkh-web --tail=100
```

### Этап 7 — Telegram-бот, позже

- [ ] Проверка разрешённого Telegram-пользователя.
- [ ] Связка web user ↔ Telegram user.
- [ ] Подключение scheduler к отправке уведомлений.
- [ ] История отправленных уведомлений.

---

## 5. Последние важные коммиты

- `e101be9` — workaround: добавить `Бекапы` в navbar через `base.html`, если шаблон забыл ссылку.
- `4f74859` — backend route для удаления backup-файла: `POST /backups/discard/{filename}`.
- `179486a` / `78b01aa` — dashboard: карточка `К оплате за выбранный месяц`.
- `2b9346e` / `b501cde` — очистка сиротских чеков и UI-кнопка в платежах.
- `1c4f4cd` — удаление/замена чеков при удалении/замене платежа.
- `a0076a1` / `8f2fcd5` — архив подрядчиков.
- `1da8b12` — не удалять подрядчика с платежами, а архивировать.
- `3b14ad1` — суммы подрядчиков через `Decimal`.
- `ef805ce` — `init_db.py` больше не создаёт `user` и не использует raw cursor.
- `ffa6896` — restore backup корректно работает с Docker volume `/app/data`.
- `68c11cc` / `f6fd83e` — restore/upload backup UI и routes.
- `288941b` / `b9dc900` — настройки backup в GUI.

---

## 6. Проверки перед коммитом

```bash
python3 -m py_compile $(find app -name '*.py') init_db.py
python -m pytest tests/ -v
docker compose up -d --build
docker ps
docker logs zhkh-web --tail=100
docker logs zhkh-bot --tail=100
```

---

## 7. Правила разработки

1. Работать в ветке `audit-dashboard-fixes`, если пользователь явно не сказал другое.
2. Перед правкой сверяться с фактическим кодом.
3. Не использовать `request.form.get()` в FastAPI handlers. Использовать явные `Form(...)`.
4. Не использовать `conn.cursor()` в миграциях SQLAlchemy async.
5. Если добавляешь миграцию, менять оба файла: `app/database.py` и `init_db.py`.
6. Делать null-safe расчёт сумм.
7. Query params парсить через `try/except`.
8. Каждый `<select>` должен иметь уникальный `id`.
9. Commit messages писать в стиле Conventional Commits.

---

# 8. Telegram-бот

Этот раздел намеренно расположен в конце файла. По текущей задаче Telegram-бот не дорабатывался.

## Назначение

Telegram-бот фиксирует оплату без захода в веб-интерфейс.

Формат сообщения:

```text
#оплачено #мосэнергосбыт #сумма:3200
```

## Файлы

```text
app/bot/
├── main.py
├── handlers.py
├── parsers.py
└── notifications.py
```

## Ограничения

- Нет полноценной проверки разрешённого Telegram-пользователя.
- Связка web user ↔ Telegram user не завершена.
- Уведомления в `app/bot/notifications.py` есть, но scheduler пока их не вызывает.
- Нужно решить защиту от дублей уведомлений.
